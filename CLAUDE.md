# הנחיות לקלוד בפרויקט הזה

## כלל יסוד: סיכום בסוף כל הודעה

**כל הודעה שלי למשתמש מסתיימת בסיכום.** בלי יוצא מן הכלל — גם הודעה קצרה,
גם תשובה לשאלה אחת, וגם עדכון סטטוס.

הסיכום הוא ציטוט (`>`) בשורה האחרונה, והוא מסביר **בקצרה את כל מה שנכתב
בהודעה** — לא רק את החלק האחרון.

**כשנושא בהודעה דורש הרחבה, הסיכום מפנה למעלה — לאן בדיוק בהודעה הוא
הורחב.** ההפניה היא לפי שם הכותרת, כדי שאפשר יהיה למצוא אותה בלי לקרוא
הכול.

### הצורה

```
[גוף ההודעה — הפירוט, הטבלאות, הקוד]

> **סיכום:** <מה נעשה או נענה, במשפט או שניים>.
> הרחבה למעלה ב"<שם הכותרת>".
```

כשאין מה להרחיב, ההפניה פשוט נשמטת והסיכום עומד לבדו.

### למה זה ככה

הפירוט קודם, הסיכום אחריו — כדי שההפניה "למעלה" תהיה נכונה. סיכום שפותח
הודעה לא יכול להפנות אל מה שעדיין לא נכתב.

---

## כלל יסוד: ענף פריסה אחד, ושמו `main`

**כל העבודה מתמזגת ל‑`main`.** זהו הענף היחיד שממנו האתר מתעדכן.

**אל תסמוך על "ענף ברירת המחדל" של GitHub כדי לדעת לאן פורסים.** הוא היה
מוגדר בעבר לענף שכלל אינו הענף הפורס, וזה הטעה. **מקור האמת היחיד הוא
`branches:` שבתוך `.github/workflows/deploy.yml`** — לבדוק שם לפני כל מיזוג.

**לעולם לא שני ענפים פורסים.** שניהם מעלים לאותה תיקייה בשרת, ו‑`index.html`
הוא של מי שפרס אחרון — כך נעלמים כלים מדף הכלים. אם מתגלה ענף נוסף עם טריגר
פריסה משלו, זו תקלה שיש לדווח עליה, לא להתעלם ממנה.

## כלל יסוד: מחזור העבודה

1. ענף משנה שיוצא מ‑`main` המעודכן
2. `commit` + `push` לענף
3. PR ל‑`main`
4. מיזוג → הפריסה מופעלת לבד
5. **לאמת `conclusion: success`**

**"ההרצה עוד רצה" אינו הצלחה, ו"התיקון אמור לעבוד" אינו אימות.** רק
`conclusion: success` בלוג.

## כלל יסוד: מגבלות ההרשאה שלי

אלה מגבלות קבועות — לא לנסות לעקוף ולא לבזבז עליהן הרצות:

| מה | התוצאה | מי כן יכול |
| --- | --- | --- |
| `workflow_dispatch`, `rerun_failed_jobs`, ביטול הרצה | 403 | המשתמש, מ‑Actions ב‑UI |
| גישה ישירה לשרת (SSH/HTTP) | חסום ברשת | רק דרך GitHub Actions |
| קריאה או כתיבה של סודות | אין כלי | המשתמש, ב‑Settings |
| שינוי הגדרות ריפו (ענף ברירת מחדל) | אין כלי | המשתמש, ב‑Settings |

כשנדרשת אחת מהן — **לומר למשתמש בדיוק מה לעשות ואיפה**, ולא להיתקע.

## כלל יסוד: `rsync` רץ בלי `--delete`

זו החלטה מכוונת. בשרת יש קבצי ריצה שאינם בריפו — הגדרות, אסימונים, מסדי
נתונים ומדיה שמשתמשים העלו — ו‑`--delete` היה מוחק אותם.

**כלי חדש שכותב נתונים בשרת חייב `--exclude` משלו ב‑`deploy.yml`.** בלעדיו
הפריסה הבאה תדרוס את נתוני המשתמשים. זו הנקודה שהכי קל לשכוח, והנזק בה
בלתי הפיך.

## כלל יסוד: כשהפריסה נכשלת

**קודם למצוא את החתימה המדויקת בלוג, ורק אחר כך לנסח תיאוריה.**

| בלוג | המשמעות |
| --- | --- |
| `Connection closed by <ip> port 22` + `code 255`, **בלי** `Permission denied` | השרת סגר את החיבור. לא המפתח ולא הקוד |
| `Permission denied` | כשל אימות — שם כן לבדוק את `DEPLOY_SSH_KEY` |

**החסימה של השרת משתחררת תוך דקות, לא שניות.** לכן ההמתנות בין הנסיונות הן
30, 90 ו‑180 שניות. המתנה קצרה מזו נופלת כולה בתוך אותו חלון חסימה, וכל
הנסיונות נכשלים זהה — מה שמדמה תקלה קבועה ומטעה.

**אם הרצה קודמת הצליחה עם אותם סודות — אין לגעת בסודות.** שינוי מפתח תקין
בגלל תקלת רשת הוא בדיוק מה שצריך למנוע.

הטבלה המלאה: `.claude/skills/deploy-to-cpanel/references/troubleshooting.md`

## כלל יסוד: מבנה של כלי חדש

```
<tool-name>/            שם באנגלית, kebab-case
  index.html            lang="he" dir="rtl"
  README.md             מה הכלי עושה, ומה טרם הוכרע
  assets/css/app.css
  assets/js/app.js
  assets/icon.svg
```

ובנוסף — **כרטיס ב‑`index.html` הראשי**, אחרת הכלי קיים בשרת אך בלתי נגיש.

## כלל יסוד: כמה צ'אטים עובדים כאן במקביל

כל סשן עובד על כלי אחר ובענף אחר, ולכן תיקייות הכלים אינן מתנגשות.
**מה שכן משותף לכולם הוא `index.html` הראשי** — ובמיוחד שורת הגרסה שבתחתיתו,
שהיא שורה אחת שכולם כותבים אליה. שני סשנים כבר לקחו אותו מספר בהפרש דקות.

**לפני נגיעה ב-`index.html` — לקרוא `.claude/COORDINATION.md`** ולהריץ את פקודת
סריקת הגרסאות שבו. **מספר גרסה נתפס במציאות ולא בכוונה**: גם ענף שטרם מוזג
כבר מחזיק מספר, ולקחת אותו שוב חוסם מיזוג.

אין ערוץ הודעות חי בין הסשנים — מה שעובר ביניהם עובר בגיט, או דרך המשתמש.

## שפה

התכתובת, הודעות ה‑commit, תיאורי ה‑PR והתיעוד בקוד — בעברית.
שמות קבצים, תיקיות וזהות בקוד — באנגלית.
