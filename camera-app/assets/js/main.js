/**
 * נקודת הכניסה. קובץ נפרד ומינימלי בכוונה:
 *
 * ה-?v= ב-index.html משמש לרענון מטמון, אבל מודול ES מזוהה לפי הכתובת
 * המלאה — `app.js?v=1` ו-`app.js` הם שני מודולים שונים לדפדפן. המסכים
 * מייבאים `./app.js` בלי ?v=, ואם גם תג ה-script היה מצביע על app.js עם
 * ?v=, הדפדפן היה מריץ את app.js פעמיים: שני boot(), שני מאזינים לכל
 * טופס, וכל כניסה נשלחת פעמיים. לכן ה-?v= יושב רק כאן, וכולם — כולל
 * הקובץ הזה — מגיעים ל-app.js באותה כתובת בדיוק.
 */
import './app.js';
